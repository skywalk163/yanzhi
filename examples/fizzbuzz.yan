# FizzBuzz — 经典面试题
# 展示：循环、条件嵌套、取余运算

打印 "=== FizzBuzz (1-15) ==="。

定义 i = 1。
循环当 i 小于 16：
  定义 m3 = 等于 取余 i 3 0。
  定义 m5 = 等于 取余 i 5 0。
  如果 m3 那么：
    如果 m5 那么：
      打印 "FizzBuzz"。
    否则：
      打印 "Fizz"。
    结束。
  否则 如果 m5 那么：
    打印 "Buzz"。
  否则：
    打印 i。
  结束。
  赋值 i = 相加 i 1。
结束。

打印 "=== 完毕 ==="。
