# 欧几里得算法 — 最大公约数 & 最小公倍数
# 展示：递归、取余、函数组合

打印 "=== 最大公约数 & 最小公倍数 ==="。

# GCD 欧几里得算法
定义 gcd = 函数 a b：
  如果 b 等于 0 那么 返回 a。
  定义 m = 取余 a b。
  gcd b m。
结束。

# LCM（用中间变量避免嵌套歧义）
定义 lcm = 函数 a b：
  定义 prod = 相乘 a b。
  定义 g = gcd a b。
  相除 prod g。
结束。

# 测试
定义 r1 = gcd 48 18。
打印 r1。
定义 r2 = gcd 1071 462。
打印 r2。
定义 r3 = lcm 12 18。
打印 r3。
定义 r4 = lcm 7 13。
打印 r4。

# 互质判断
定义 coprime = 函数 a b：
  定义 g = gcd a b。
  等于 g 1。
结束。
定义 r5 = coprime 7 13。
打印 r5。
定义 r6 = coprime 12 18。
打印 r6。

打印 "=== 完毕 ==="。
