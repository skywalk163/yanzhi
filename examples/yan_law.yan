# 言律自然语法示例
# 展示：要是句式、作用域块、循环当句式

打印 "=== 言律自然语法 ==="。

# --- 要是句式 ---
打印 "--- 要是句式 ---"。
定义 score = 85。
要是 score 大于 90，就 打印 "优秀" 否则 打印 "非优秀"。

# --- 作用域块 ---
打印 "--- 作用域块 ---"。
回家的时候：
  打印 "换鞋"。
  打印 "洗手"。
  打印 "休息"。

# --- 循环当句式 ---
打印 "--- 循环当句式 ---"。
循环当 真，就 打印 "循环体执行一次"。

打印 "=== 完毕 ==="。
