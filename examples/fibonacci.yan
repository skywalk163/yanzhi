# 斐波那契数列 — 递归实现
# 展示：递归、块语法、条件判断、数学运算

打印 "=== 斐波那契 ==="。

定义 fib = 函数 n：
  如果 n 小于 2 那么 返回 n。
  定义 t1 = 相减 n 1。
  定义 t2 = 相减 n 2。
  定义 a = fib t1。
  定义 b = fib t2。
  相加 a b。
结束。

定义 i = 0。
循环当 i 小于 11：
  定义 val = fib i。
  打印 val。
  赋值 i = 相加 i 1。
结束。

打印 "=== 完毕 ==="。
