# 冒泡排序 — 言知语言实现
# 使用 若...则：... 语句块语法

定 冒泡排序 = 函 lst：
  定 n = 长 lst。
  定 i = 0。
  当 i 小 n - 1：
    定 j = 0。
    定 t = 0。
    当 j 小 n - 1 - i：
      若 lst[j] 大 lst[j + 1] 则：
        设 t = lst[j]。
        设 lst[j] = lst[j + 1]。
        设 lst[j + 1] = t。
      。
      设 j = j + 1。
    。
    设 i = i + 1。
  。
  lst。
。

定 data = 列 5 3 8 1 9 2 7 4 6。
印 "排序前："。
印 data。
定 r = 冒泡排序 data。
印 "排序后："。
印 r。
