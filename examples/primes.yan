# 质数判断与筛法
# 展示：递归、条件、取余、循环

打印 "=== 质数判断 ==="。

# 判断 n 是否质数（特判2）
定义 is_prime = 函数 n div：
  如果 n 等于 2 那么 返回 真。
  如果 n 小于 2 那么 返回 假。
  如果 等于 取余 n div 0 那么 返回 假。
  定义 sq = 相乘 div div。
  如果 sq 大于 n 那么 返回 真。
  定义 nd = 相加 div 1。
  is_prime n nd。
结束。

# 打印 1-30 中的质数
打印 "1-30 中的质数："。
定义 i = 2。
循环当 i 小于 31：
  定义 ok = is_prime i 2。
  如果 ok 那么 打印 i。
  赋值 i = 相加 i 1。
结束。

打印 "=== 完毕 ==="。
