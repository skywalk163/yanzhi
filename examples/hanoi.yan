# 汉诺塔 — 言知语言实现
# 使用 那么：语句块语法

定义 汉诺 = 函数 n 源 目标 辅助：
  如果 n 等于 1 那么：
    打印(源 + " -> " + 目标)。
  否则：
    汉诺(n - 1，源，辅助，目标)。
    汉诺(1，源，目标，辅助)。
    汉诺(n - 1，辅助，目标，源)。
  。
。

打印 "汉诺塔(3)："。
汉诺 3 "A" "C" "B"。
打印 "完成"。
