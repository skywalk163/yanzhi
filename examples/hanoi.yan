# 汉诺塔 — 言知语言实现
# 使用 则：语句块语法

定 汉诺 = 函 n 源 目标 辅助：
  若 n 等 1 则：
    印(源 + " -> " + 目标)。
  否则：
    汉诺(n - 1，源，辅助，目标)。
    汉诺(1，源，目标，辅助)。
    汉诺(n - 1，辅助，目标，源)。
  。
。

印 "汉诺塔(3)："。
汉诺 3 "A" "C" "B"。
印 "完成"。
