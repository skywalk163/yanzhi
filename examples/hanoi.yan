# 汉诺塔 — 言知语言实现
# 使用全局变量避免多参数调用问题

定义 源 = "A"。
定义 目标 = "C"。
定义 辅助 = "B"。

定义 hanoi = 函数 n：
  如果 n 等于 1 那么：
    打印 源。
    打印 "->"。
    打印 目标。
    返回 空。
  结束。
  定义 n1 = 相减 n 1。
  hanoi n1。
  打印 "移动盘子 " + 字 n。
结束。

hanoi 3。
