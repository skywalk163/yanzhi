# -*- coding: utf-8 -*-
"""比较基本库

提供组合比较动词：不等、小等于、大等于等。
这些可以用核心比较动词（大、小、等）+ 逻辑动词（非）组合实现，
但作为基本库提供更简洁的调用方式。

用法：
  3不等4  → 真
  3小等于3 → 真
  5大等于3 → 真
"""


LIBS = {
    '不等': (2, lambda a, b: a != b),
    '小于等于': (2, lambda a, b: a <= b),
    '大于等于': (2, lambda a, b: a >= b),
}
