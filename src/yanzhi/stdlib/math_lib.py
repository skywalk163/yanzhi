# -*- coding: utf-8 -*-
"""数学基本库

提供三角函数、指数对数、取整等数学函数。
这些函数不是语言核心关键字，而是通过基本库机制注册。

用法：在知行语言中直接调用
  正弦 0.5
  余弦 3.14
  开方 16
  绝对值 负7
  取整 3.7
"""
import math


# 每个基本库模块导出 LIBS 字典：
#   key = 动词名（中文）
#   value = (元数, 函数)

LIBS = {
    # 三角函数
    '正弦': (1, math.sin),
    '余弦': (1, math.cos),
    '正切': (1, math.tan),
    # 指数对数
    '指数': (1, math.exp),
    '对数': (1, math.log),
    # 幂和根
    '开方': (1, math.sqrt),
    # 取整
    '绝对值': (1, abs),
    '取整': (1, lambda x: int(x)),
    '四舍五入': (1, round),
}
